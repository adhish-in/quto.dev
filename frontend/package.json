{
  "scripts": {
    "dev": "set PORT=4000 && react-scripts start",
    "start": "npm run build && serve -s build -l 80",
    "build": "react-scripts build"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "dependencies": {
    "react-scripts": "^5.0.1",
    "serve": "^14.2.4"
  }
}